name: chatOps
on:
  issue_comment:
    types: [created]

jobs:
  chatopt:
    uses: hudeng-go/.github-1/.github/workflows/chatOps.yml@master
    secrets:
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
